import { CommandContext } from 'grammy';
import type { MyContext } from '../index';

export async function helpCommand(ctx: CommandContext<MyContext>) {
  const user = ctx.from;
  const name = [user?.first_name, user?.last_name].filter(Boolean).join(' ');
  const username = user?.username ? `@${user.username}` : '-';
  const telegramId = user?.id || 0;
  
  const helpMessage = `مرحباً بك في EasyTransfer 2.0! 👋

💸 إرسال التحويلات:
━━━━━━━━━━━━━━━━

📱 الطريقة التفاعلية:
استخدم الأمر /send ثم اتبع التعليمات خطوة بخطوة

⚡ الطريقة السريعة:
/send <المبلغ> <رقم الهاتف>

مثال:
/send 1000 0912345678

💰 الاستعلام عن الرصيد:
━━━━━━━━━━━━━━━━
استخدم الأمر /balance واختر المشغل (سيريتل أو MTN)

📋 الأوامر المتاحة:
━━━━━━━━━━━━━━━━
/start - عرض معلومات حسابك
/send - إرسال تحويل
/balance - الاستعلام عن الرصيد
/help - عرض هذه الرسالة
/health - حالة البوت (للمطورين)

ℹ️ ملاحظات مهمة:
━━━━━━━━━━━━━━━━
• يجب أن تكون مسجلاً في النظام لاستخدام البوت
• هناك قيود على المبالغ المسموح تحويلها
• يجب الانتظار 5 دقائق بين التحويلات لنفس المستلم
• يجب الانتظار 20 ثانية بين التحويلات المختلفة

للمساعدة أو الدعم، تواصل مع مدير النظام.`;
  
  await ctx.reply(helpMessage);
}
